DEV_API_URL=http://localhost:3000
API_URL=https://pryladova.duckdns.org
# Remote profile: --remote flag or AGENT_PROFILE=remote (uses API_URL + requires INGEST_SECRET)
POLL_INTERVAL_MS=2000
INGEST_SECRET=
# Optional: comma-separated app names merged with the default blocklist
# BLOCKED_APPS=customapp,anotherapp
# Remote API: pnpm dev:agent:remote (or agent start:remote) — uses API_URL
