# Optional — leave empty to disable Sentry locally.
# Production: set VITE_SENTRY_DSN in GitHub Actions (CI workflow builds the SPA).
# VITE_SENTRY_RELEASE is build/CI only (vite.config.ts); the bundle reads VITE_APP_RELEASE.
VITE_SENTRY_DSN=
VITE_SENTRY_RELEASE=
