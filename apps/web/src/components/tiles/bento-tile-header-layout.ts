/** Shared bento tile header shell — single row, fixed inner height. */
export const bentoTileHeaderClassName = "shrink-0 border-b px-(--card-spacing) py-2.5";

/** Inner row height shared by every bento tile header. */
export const bentoTileHeaderRowClassName = "flex h-5 min-w-0 items-center gap-2";

/** Lucide icons in bento headers — muted like pre-BentoTileHeader tiles. */
export const bentoTileLucideIconClassName = "size-3.5 text-muted-foreground";

export const bentoTileHeaderTestIdSuffix = "-tile-header";
