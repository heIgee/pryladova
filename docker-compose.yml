services:
  api:
    image: ghcr.io/${GHCR_OWNER}/pryladova-api:${IMAGE_TAG}
    container_name: pryladova-api
    restart: unless-stopped
    env_file:
      - .env
    environment:
      SENTRY_RELEASE: ${SENTRY_RELEASE:-}
    ports:
      - "127.0.0.1:3000:3000"
