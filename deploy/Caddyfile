{$PRYLADOVA_DOMAIN} {
	# API and WebSockets — Nest enforces panel and agent authentication.
	handle /api/* {
		reverse_proxy 127.0.0.1:3000
	}

	# Static web SPA — login handled by the app
	handle {
		root * {$PRYLADOVA_WEB_ROOT}
		encode gzip
		try_files {path} /index.html
		file_server
	}
}
